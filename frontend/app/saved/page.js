import SavedClient from "./SavedClient";

export const metadata = {
  title: "Saved Words — சொல் AI",
  description: "View your saved Tamil words.",
};

export default function SavedPage() {
  return <SavedClient />;
}
